import { FormConfigDirective } from './form-config.directive';

describe('FormConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new FormConfigDirective();
    expect(directive).toBeTruthy();
  });
});
