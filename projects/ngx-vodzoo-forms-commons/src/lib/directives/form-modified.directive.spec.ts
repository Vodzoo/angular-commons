import { FormModifiedDirective } from './form-modified.directive';

describe('FormModifiedDirective', () => {
  it('should create an instance', () => {
    const directive = new FormModifiedDirective();
    expect(directive).toBeTruthy();
  });
});
