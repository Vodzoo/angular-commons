import { FormEventsDirective } from './form-events.directive';

describe('FormEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new FormEventsDirective();
    expect(directive).toBeTruthy();
  });
});
